# DB test riêng — KHÔNG dùng chung với DB dev (npm run dev / run_locallhost.ps1).
# jest globalSetup tự tạo database này trong cùng cluster Postgres portable, áp migration,
# rồi TRUNCATE sạch trước mỗi lần chạy suite. Xem skills/build/local-dev-environment/SKILL.md § 9.
# Chỉ chứa cấu hình localhost, không có bí mật — commit được.
DATABASE_URL=postgresql://postgres:postgres@localhost:5436/fsolution_test?schema=public
NODE_ENV=test
JWT_SECRET=test-secret-fsolution
