NODE_ENV=development
PORT=3003
FRONTEND_URL=http://localhost:5176
# Local dev (infra/docker-compose.yml) hoặc Supabase connection string
DATABASE_URL=postgresql://postgres:postgres@localhost:5436/fsolution?schema=public
# Ký JWT access-token (8h, không refresh) — đổi giá trị thật khi triển khai production
JWT_SECRET=dev-secret-fsolution-doi-khi-len-production
